---
name: prereq-and-objectives
description: 单主题深读的「读前准备」环节：为一份阅读材料（论文/源码/教材）生成「前置知识文档 + 阅读目标问题」。前置知识 = (材料所需工具/概念集合) − (背景文件已具备集合) + 少量桥接补充；背景文件路径由环境变量 $PERSONAL_BACKGROUND 指定。阅读目标 = 3–5 个"读完要能回答"的核心问题。用于把被动阅读变成带靶子的主动阅读，并按"读前置→读正文"安排顺序。是 qa_note.md 之前的一步。
---

# 读前准备：前置知识（差集）+ 阅读目标问题

> **定位**：单主题深读流程的第一步，在随文 QA（`qa_note.md`）之前。
> **认知依据**：先行组织者（advance organizer）降低首次阅读的认知负荷；预设目标问题制造"认知缺口"，让阅读带靶子、主动提取，而非顺着读一遍。

---

## 0. 目标（一句话）

读正文之前，先产出两样东西：
1. **前置知识文档**：补齐"这份材料默认你已会、但你其实还缺"的那部分概念——**精确的差集，不是通用综述**。
2. **阅读目标问题**：3–5 个"读完这份材料要能回答"的核心问题，作为阅读靶子。

---

## 1. 前置知识 = 差集 + 桥接（核心公式）

```
前置知识文档  =  (本材料所需工具/概念集合  S_need)
              −  (背景文件已具备集合         S_have)
              +  少量桥接补充               S_bridge
```

> **背景文件**：路径由环境变量 `$PERSONAL_BACKGROUND` 指定（作者维护的知识资产台账）。下文凡提到读取 `S_have`，都指读这个文件。

- **`S_need`（LLM 抽取）**：通读材料（或其摘要/目录/关键公式），列出读懂它**默认要会**的概念、记号、方法、工程前提。
- **`S_have`（读背景文件 `$PERSONAL_BACKGROUND`）**：作者已具备的底子，以背景文件实际内容为准（稳定区的身份/底子 + 知识资产台账逐轮累积的已会项）。**凡落在 `S_have` 内的，一律不写**——不浪费篇幅讲作者已会的。
  - **重点读「知识资产台账」区**：那里逐主题累积了历轮流水线新增的 `S_have`（由 `background_update.md` 写入），是比稳定区更新鲜、更细颗粒的已会清单。台账里标了「仍存疑/未深入」的项，**不要**当作已会——这些要进 `S_bridge`。台账越长，差集越准，这是流程闭环的收益。
- **`S_bridge`（少量补充）**：差集里"作者虽没系统学过、但只差一层窗户纸"的点，给最小桥接（一段话/一个类比/一条记号约定），不展开成小教材。

**硬边界：**
- ❌ 不写通用百科式综述，不把 `S_have` 里的东西复述一遍。
- ❌ 不深挖差集里的每一项到底——前置文档是"够读正文即可"的脚手架，不是第二篇论文。
- ✅ 差集判断不确定时，宁可在条目后标注"（不确定你是否已掌握，若已会可跳过）"，也不要默认作者不会。

---

## 2. 阅读目标问题（3–5 个）

读完材料后，作者应能回答的核心问题。要求：

- 命中**这份材料独有的"为什么/怎么做到"**，而非泛泛的"它是什么"。
- 与作者已有知识形成对照钩子（如"它和 MTR/VectorNet 在 X 上有何不同、为什么"）。
- 后续随文 QA 和复习卡都可回指这些目标问题，检验是否真读懂。

---

## 3. 阅读顺序

产出后，按 **读前置知识文档 → 读正文（随文跑 `qa_note.md` 的 Q&A）** 推进。前置文档建议命名 `{source}_prereq.md`，与材料同级目录，并在 obsidian 枢纽「前置知识」区链接。

---

## 4. 产出格式

```markdown
# {材料名} — 读前准备

## A. 前置知识（差集）
> 已剔除背景文件中已具备项；仅列你为读这份材料还缺的部分。
### A1. <概念/记号>
- 为什么读这份材料需要它：…
- 最小够用解释：…
### A2. …

## B. 阅读目标问题（读完要能回答）
1. …
2. …
```

---

## 5. 开工前应确认

- 材料路径（用于 `S_need` 抽取与 `{source}_prereq.md` 落位）。
- 是否有作者自知的薄弱点想重点补（可加入 `S_bridge`）。
- 若背景文件（`$PERSONAL_BACKGROUND`）某些项已过时，以作者口头修正为准。
