---
name: prereq-and-objectives
stage: B/prereq
when: 材料已确定，读正文前出前置差集 + 目标问题
reads: 选定材料 + baseline.yaml + s_have(have-query)
writes: {source}_prereq.md
description: 单主题深读的「读前准备」环节：为一份阅读材料（论文/源码/教材）生成「前置知识文档 + 阅读目标问题」。前置知识 = (材料所需工具/概念集合) − (已具备集合 S_have) + 少量桥接补充；S_have 从结构化背景取（baseline.yaml 基线 + background.py have-query 按领域查 s_have 表，见 background_db.md）。阅读目标 = 3–5 个"读完要能回答"的核心问题。用于把被动阅读变成带靶子的主动阅读，并按"读前置→读正文"安排顺序。是 qa_note.md 之前的一步。
---

# 读前准备：前置知识（差集）+ 阅读目标问题

> **定位**：单主题深读流程的读前准备步，在随文 QA（`qa_note.md`）之前。**上游是 `source_selection.md`（`collect` 步）**：它已把 theme 收敛成"确定的、范围可控的资料清单"`{topic}_sources.md`；本步从那份清单选定且裁剪的材料里抽 `S_need`。
> **认知依据**：先行组织者（advance organizer）降低首次阅读的认知负荷；预设目标问题制造"认知缺口"，让阅读带靶子、主动提取，而非顺着读一遍。
> **前提**：材料必须先经 `source_selection.md` 确定。若材料本就给定（如直接拿到一篇论文），可跳过 collect 直接进本步；若来自 topic_map 拆出的 theme，则**必须先跑 collect**——否则没有材料，`S_need` 无从抽，差集链断在起点。

---

## 0. 目标（一句话）

读正文之前，先产出两样东西：
1. **前置知识文档**：补齐"这份材料默认你已会、但你其实还缺"的那部分概念——**精确的差集，不是通用综述**。
2. **阅读目标问题**：3–5 个"读完这份材料要能回答"的核心问题，作为阅读靶子。

---

## 1. 前置知识 = 差集 + 桥接（核心公式）

```
前置知识文档  =  (本材料所需工具/概念集合  S_need)
              −  (背景已具备集合（基线/台账）  S_have)
              +  少量桥接补充               S_bridge
```

> **背景（结构化）**：`$PERSONAL_BACKGROUND` 现在指向 workspace 的 `background/` 目录。`S_have` 不再"读一个 md 文件"，而是两路并取：① **基线锚点**直接读 `baseline.yaml`（身份/出身/入职基线，差集的地平线）；② **累积台账**用 `background.py have-query` 从 SQLite 按领域取。schema 与 CLI 见 `background_db.md`。

- **`S_need`（LLM 抽取）**：通读**材料**——即 `source_selection.md` 产出的 `{topic}_sources.md` 里**选定且裁剪过**的那几份（读其指定的节/页/函数，或其摘要/目录/关键公式）——列出读懂它**默认要会**的概念、记号、方法、工程前提。范围裁剪让 `S_need` 聚焦，不被无关章节稀释。
- **`S_have`（基线 + 结构化台账）**：作者已具备的底子 = `baseline.yaml` 基线锚点 ∪ `s_have` 表里历轮累积的已掌握项。**凡落在 `S_have` 内的，一律不写**——不浪费篇幅讲作者已会的。
  - **台账按领域分区查，别全量翻**：先判断本材料涉及哪些领域，再 `python $env:LEARNING_PIPELINE\scripts\background.py have-query --domain <领域1> <领域2>` 只取相关领域的 `S_have`（治"主题越多差集越慢/越不准"）。
  - **`shaky` 项不算已会**：`have-query` 把 `status: shaky`（"仍存疑/未深入"）显式标出——这些**不要**当 `S_have` 跳过，要进 `S_bridge`。
  - 台账由 `background_update.md` / `weekly_review.md` 幂等写入（`have-add`），领域越全、颗粒越细，差集越准——这是流程闭环的收益。「能力现状表」只是 `have-table` 生成的视图，不是独立来源（不再有"以台账为准"的人肉纪律）。
- **`S_bridge`（少量补充）**：差集里"作者虽没系统学过、但只差一层窗户纸"的点，给最小桥接（一段话/一个类比/一条记号约定），不展开成小教材。

**硬边界：**
- ❌ 不写通用百科式综述，不把 `S_have` 里的东西复述一遍。
- ❌ 不深挖差集里的每一项到底——前置文档是"够读正文即可"的脚手架，不是第二篇论文。
- ✅ 差集判断不确定时，宁可在条目后标注"（不确定你是否已掌握，若已会可跳过）"，也不要默认作者不会。

---

## 2. 阅读目标问题（3–5 个）

读完材料后，作者应能回答的核心问题。要求：

- 命中**这份材料独有的"为什么/怎么做到"**，而非泛泛的"它是什么"。
- 与作者已有知识形成对照钩子（如"它和 MTR/VectorNet 在 X 上有何不同、为什么"）。
- 后续随文 QA 和复习卡都可回指这些目标问题，检验是否真读懂。

---

## 3. 阅读顺序

产出后，按 **读前置知识文档 → 读正文（随文跑 `qa_note.md` 的 Q&A）** 推进。前置文档命名 `{source}_prereq.md`（`{source}`=`topics.source`），与材料同级目录；hub「前置知识」链接由 `topic-render` 自动生成（无需手链）。

---

## 4. 产出格式

```markdown
# {材料名} — 读前准备

## A. 前置知识（差集）
> 已剔除背景（基线 + `s_have` 台账）中已具备项；仅列你为读这份材料还缺的部分。
### A1. <概念/记号>
- 为什么读这份材料需要它：…
- 最小够用解释：…
### A2. …

## B. 阅读目标问题（读完要能回答）
1. …
2. …
```

---

## 5. 开工前应确认

- 材料是否已经 `source_selection.md` 确定（来自 theme 的必须先有 `{topic}_sources.md`；材料给定的可直接给路径）。
- 材料路径（用于 `S_need` 抽取与 `{source}_prereq.md` 落位）。
- 是否有作者自知的薄弱点想重点补（可加入 `S_bridge`）。
- 若背景（`baseline.yaml` 基线 / `s_have` 台账）某些项已过时，以作者口头修正为准（必要时先 `background.py have-add` 幂等更正）。
