---
name: learning-qa-note-append
description: 学习随文问答的产出规范：把用户问题原文 + 助手最终回答原文，落成阅读材料同级 {source}_qa/ 文件夹下、每个 QA 一个独立 .md 文件，带 YAML front-matter（机器验证字段 id/content_hash 由脚本管、LLM 只读；导航字段 title/summary/questions/chapter_hint/related 由 LLM 写）。正文区是纯净原文、禁止任何生成式改写，content_hash 由 qa_archive.py finalize 锁定。下游归档(qa_to_archive.md)只读导航字段做编排、由脚本字节级搬运正文，从根上杜绝"LLM 机械搬运改字"的幻觉。必要时回答中可用 Mermaid，随回答原样进入正文区。
---

# 学习 Q&A 笔记：单文件 + front-matter 规范

> **用途**：用户围绕某份本地阅读材料学习提问时，助手正常回答，并把"问题原文 + 回答原文"落成 `{source}_qa/` 文件夹下**每个 QA 一个独立 `.md` 文件**。
> **本文件定位**：可手动调用的 prompt 规范（非 skill）。
> **在流程中的位置**（见 `README.md`）：上游 `prereq_and_objectives.md`（读前准备）→ **本步（随文 QA，产出单文件）** → 下游 `qa_to_archive.md`（LLM 出编排表，脚本保真拼接归档）。本步产出的 QA 文件同时是下游归档与 `to_review_cards.md` 复习卡的原料，因此**正文原样保真**尤其重要。
> **为什么是单文件 + 哈希**：纯机械搬运不该由 LLM 承担——那是脚本能 100% 可靠做的事。把每个 QA 切成独立文件、用 `content_hash` 锁正文，下游只让 LLM 做"去哪、排第几"的编排判断，正文由脚本按 ID 字节级搬运，从工程上锁死"篡改原文"这个最危险的失败模式。

---

## 0. 目标（一句话）

把学习过程中的每一次问答，落成**与阅读材料绑定、可机器校验、可持续累积的单文件 QA**：用户当时怎么问、助手最终怎么答，正文区就逐字保存什么。

**两个不可动摇的落点：**
- **回答优先**：先完成高质量解释，再落文件；不要为了归档牺牲回答本身的清晰度。
- **正文保真**：进正文区的是用户问题原文与助手最终回答原文，不改写、不摘要、不润色。

---

## 1. 触发场景与输入性质

- 用户正在阅读某份本地材料（论文 PDF、翻译稿、教材笔记、源码导读、学习计划等）。
- 用户围绕该材料提出概念/公式/算法/代码/图示/工程实现或理解偏差方面的问题。
- 用户希望这些问答随学习进展自动沉淀，而非散落在聊天上下文。

**重要判断**：只要本 prompt 已被手动调用、且问题明显属于学习问答，就默认需要落文件，不要每次重复询问"是否记录"。

---

## 2. QA 文件夹位置与命名

笔记位置由**阅读材料位置**决定：

- 材料是单个文件 → QA 文件夹放在该文件同级目录。
- 材料是一个目录 → QA 文件夹放在该目录内。
- 多份材料同目录 → 用共同目录。
- 路径无法从上下文确定 → 先向用户确认材料路径。

**文件夹名**：`{source}_qa/`（`{source}` = 材料文件名去扩展名的主体；材料是目录则用目录名）。

> **预习组 vs 主组（同一 topic 内两段分离）**：「预习教材」（`prereq_to_textbook.md` 产出）与「主材料」是**两个不同的 `{source}`**，因此天然落到**两个不同的 `{source}_qa/`**——预习组用 `{topic}_预习_*` 命名空间（读预习教材时同步 QA），主组用 `{topic}_*`。**两组绝不并入同一份归档**：预习是跨领域基础底子、非完整 topic，分离才能让下游 `background_update.md` 的能力画像分清"基础底子 vs topic 专属"。

**单个 QA 文件名**：`QA_NNNN.md`（4 位零填充，如 `QA_0007.md`）。
- 新建时取文件夹内现有 `QA_*.md` 的**最大编号 + 1**。
- **绝不复用/覆盖已存在的文件名**（覆盖会悄悄毁掉旧 QA，且 verify 抓不到）。

示例：
```text
阅读材料：  E:\...\SIMPL\2402.02519v1.pdf
QA 文件夹： E:\...\SIMPL\2402.02519v1_qa\
单个 QA：   E:\...\SIMPL\2402.02519v1_qa\QA_0007.md
```

---

## 3. 单文件格式（YAML front-matter + 纯净正文）

每个 QA 文件如下。**front-matter 的两类字段权限不同，必须分清（见 §4）：**

```md
---
# ── 机器验证字段（脚本写/验，LLM 只读，禁改）──
id: QA_0007            # = 文件名主体；新建时取现有最大编号 +1
content_hash:          # 留空！由 qa_archive.py finalize 写入，LLM 绝不填写/修改
source: 2402.02519v1   # 来源材料 {source}
status: draft          # 由 finalize 置为 final（正文随之锁定）
# ── 导航字段（LLM 写，给人和下游编排看，错了可见可审）──
title: <一句话标题>
summary: <压缩摘要：供下游排逻辑链时快速理解该 QA 讲了什么，不替代正文>
questions: [<本 QA 涉及的提问要点>]
chapter_hint: <章节归属建议，仅供下游编排参考>
related: [<关联 QA id，如 QA_0003>]
---
## Q

<用户问题原文>

## A

<助手最终回答原文（可含 Mermaid），纯净，禁止任何改写>
```

**格式边界：**
- 正文区只放 `## Q` / `## A` 两段原文；不加日期/标签/总结到正文（这些进导航字段）。
- 每个 QA 独立成文件；不在文件间追加、不重排历史文件。
- 文件夹不存在则创建。

---

## 4. 字段权限（最关键，决定保真链是否成立）

| 类别 | 字段 | 谁写 | LLM 权限 |
|---|---|---|---|
| **机器验证（保真）** | `content_hash` | `qa_archive.py finalize` | **只读，绝不填/改** |
| **机器验证（保真）** | `id` | LLM 新建时按"最大+1"给一次（= 文件名）；之后只读 | 新建时写一次，之后只读 |
| 控制 | `status` | finalize 置 final | 新建写 `draft`，之后只读 |
| 导航 | `title`/`summary`/`questions`/`chapter_hint`/`related` | LLM | 可写、可后续修订（不破坏保真链） |

> **铁律**：`content_hash` 是脚本对**正文区**算的哈希，是检测"正文被悄悄改"的唯一可靠手段。LLM 在任何环节都**不得填写或修改它**——新建时留空，由 `finalize` 写入。改导航字段不影响哈希（哈希只覆盖正文区），但**绝不要顺手"补全"或"重算" content_hash**。

---

## 5. 回答与正文的边界

**可以做：** 回答中进行必要解释、推导、对比、例子、代码、表格、公式、列表；必要时用 Mermaid 帮助理解（图示属回答正文的一部分，原样进正文区）。

**不能做：** 不把问题"标准化"后入档；不把回答压缩成摘要后入正文（摘要进 `summary` 导航字段）；不把中间草稿/工具输出/过程性想法写进正文（除非出现在最终回答中）；不为格式改动用户原问题或最终回答。

一句话：**聊天里最终交付给用户什么，正文区的 A 就保存什么；用户原文是什么，正文区的 Q 就保存什么。** 提炼归提炼，但提炼只进 `summary`，不进正文。

---

## 6. 默认工作流

1. **识别 QA 文件夹**：从材料路径确定 `{source}_qa/`。
2. **回答问题**：正常完成学习问答，必要时用公式/代码/图示。
3. **落单文件**：取新 `id`（最大+1），写 front-matter——导航字段如实填、`content_hash` 留空、`status: draft`——正文区放纯净 `## Q`/`## A` 原文。
4. **定稿锁哈希**（会话或批次收尾时）：
   ```powershell
   python $env:LEARNING_PIPELINE\scripts\qa_archive.py finalize <材料目录>\{source}_qa
   ```
   脚本对每个文件正文区算哈希、写入 `content_hash` 并置 `status: final`。可随时
   `python $env:LEARNING_PIPELINE\scripts\qa_archive.py verify <...>\{source}_qa` 复核全部保真。
   （`$LEARNING_PIPELINE` = 本 pipeline 仓库的 clone 路径，见 README「使用方式」。）
5. **告知结果**：简要说明落到了哪个文件夹、新增了哪些 QA id、是否已 finalize。

---

## 7. 需要向用户确认的情况

仅在以下情况先问用户：
- 无法判断阅读材料路径。
- 同一上下文出现多个候选材料目录，且没有明显主材料。
- 用户要求非默认的文件夹名或归档规则。

除此之外，默认自动执行"回答 + 落单文件"。
