---
name: prereq-to-textbook
description: 编辑层「预习这一路」：把 prereq_and_objectives.md 产出的「前置知识差集脚手架」（{source}_prereq.md）扩写成一份"读正文前能独立学一遍的预习小册子"内容，再交通用渲染层 tool_prompts/to_tex.md 渲染为可编译 .tex。与「笔记这一路」note_to_textbook.md 对称。核心是两个正交控制轴：学习范围守最小脚手架（不外扩成第二篇论文），讲解深度按重要性分流（基础概念点到为止，理解论文关键洞察的重要前置必须按逻辑链展开+联动论文对应关键点）。本文件只管"把差集组织成预习内容"；LaTeX 样式/编译/通用「教科书味」写作标准见 tool_prompts/to_tex.md。
---

# prereq → 预习教材内容：预习编辑层改写规范

> **定位**：这是**编辑层**里「预习这一路」的规范，与「笔记这一路」`note_to_textbook.md` 对称。本文件只管**把前置知识差集组织成可学的预习内容**；**怎么渲染成 .tex（预导言/TikZ/画图避坑/编译自检）以及通用「教科书味」写作标准，全部见渲染层 `tool_prompts/to_tex.md`**。
> **用法：把本文件 + `tool_prompts/to_tex.md` 一起作为上下文。** 本文件管「差集怎么扩写、扩到什么深度、按什么边界」，`to_tex.md` 管「怎么写得有教科书味 + 排版/绘图/编译」。
> **在流程中的位置**（见 `README.md`）：上游 `prereq_and_objectives.md` 产出 `{source}_prereq.md`（差集 + 目标问题，最小脚手架）→ **本步（把预习也做成可复习教材）** → 渲染层 `tool_prompts/to_tex.md`。这是 prereq 的**可选旁分支**（类比 `to_review_cards.md`），产出在 obsidian 枢纽 `topic_hub.md`「前置知识」区链接。

---

## 0. 目标（一句话）

把 `prereq_and_objectives.md` 产出的**差集脚手架**，扩写成一份**读正文前能独立学一遍、并直接为读懂论文铺路**的预习小册子内容——既不膨胀成第二篇论文，又能让真正重要的前置项被掰开讲透、连上论文的关键洞察。

**两个不可动摇的落点：**
- **范围守脚手架**：内容边界仍是差集本身，以"够你完成主任务学习 / 读懂这篇论文"为界，不外扩、不复述正文。
- **深度按重要性分流**：不是统一扩写——基础概念点到为止，理解论文关键洞察的重要前置必须按逻辑链展开并联动论文。

---

## 1. 输入

- `{source}_prereq.md`（`prereq_and_objectives.md` 的产出：A. 前置知识差集 + B. 阅读目标问题）。
- 可选：原材料（论文/源码/教材）的摘要、目录、关键公式——用于判断"哪个前置项支撑论文的哪个关键洞察"，做 §3 的论文联动。
- 背景文件（`$PERSONAL_BACKGROUND`）：差集已剔除 `S_have`，本步沿用，不重复讲作者已会的。

---

## 2. 与 `prereq_and_objectives.md` 的分界（两份各守一条线）

| | `prereq_and_objectives.md`（上游脚手架） | `prereq_to_textbook.md`（本步预习教材） |
|---|---|---|
| 职责 | 判定差集 + 出 3–5 个目标问题 | 把差集**扩写成可学的预习内容** |
| 深度 | 够读正文即止，**不展开成小教材** | 重要项按逻辑链展开、联动论文 |
| 产物 | `{source}_prereq.md`（清单式） | 预习教材 `.tex`（经 to_tex 渲染） |

> **关键边界**：扩写发生在**本步**，不要回头去膨胀 `prereq_and_objectives.md`——它必须保持"最小脚手架"哲学，那是它喂给本步的前提。本步也**不新增差集之外的前置项**：扩的是"已判定前置项"的讲解深度，不是范围。

---

## 3. 两个正交控制轴（最关键）

### 轴 1 · 学习范围（守住）

内容边界 = 差集本身。以"够完成主任务学习 / 读懂论文"为界：不复述正文、不外扩成第二篇论文、不引入差集之外的新前置项。继承上游的克制。

### 轴 2 · 讲解深度（按重要性分流）

| 前置项性质 | 讲解深度 | 形式 |
|---|---|---|
| **基础概念**（你只差"认识一下"就能过） | 点到为止，给基础概念讲解即可 | 简短定义 + 一句直觉（`notebox`） |
| **重要内容**（理解论文关键洞察的支点） | **必须按逻辑链完整展开**：不是列概念，而是掰开讲"它是怎么来的"（动机/推导），逻辑顺畅、讲解到位；**并联动论文对应关键点** | 正文推导链 + 指向论文的钩子 |

**分档判据**：这个前置项是不是理解论文关键洞察的**支点**？
- 是 → 逻辑链展开 + **论文联动**。
- 否 → 基础讲解即可，别浪费篇幅。

### 论文联动（这一路独有）

重要前置项展开时，要**显式钩到"它在论文里支撑了哪个关键点/洞察"**——例如「这个表示的对称性，正是论文 §X 之所以能做到视角无关的根因」。目的：预习**向前服务于读正文**，不是一本孤立的小百科。

> **与 `note_to_textbook.md` 的根本区别**：笔记路是**回看**已发生的 QA 认知轨迹、忠实全量内化；预习路是**前瞻**、选择性深度、为论文铺路。本文件只定"哪些项配得上逻辑链展开 + 必须联动论文"这条来源专属取舍；"逻辑链怎么写得顺"是通用「教科书味」标准，见 `to_tex.md §6`。

---

## 4. 组织

- **重要项问题驱动展开**：先抛"读正文为什么需要它 / 论文哪里要用到它"，制造认知缺口，再按逻辑链补上——遵循 `to_tex.md §6` 的教科书味标准。
- **基础项就近收束**：简短定义 + 直觉即可，可成组放进一节，不单独大篇幅铺开。
- **目标问题作"预习自测"**：把 `{source}_prereq.md` 的 3–5 个阅读目标问题放末尾一节，作为"读正文前先自检预习是否到位"的靶子（与正式复习卡 `to_review_cards.md` 不同，这里是读前自检）。

---

## 5. 渲染 / 编译 / 样式 → 见渲染层

全部遵循渲染层 `tool_prompts/to_tex.md`，本文件不重复排版规范：
- 通用「教科书味」写作标准见其 §6；
- 预导言、固定 TikZ 样式、画图避坑见其 §1/§2/§3；
- 编译自检见其 §5（XeLaTeX 连两遍 + 查 `.log` + 确认 PDF）。
- 产物命名建议 `{source}_预习_Textbook.tex`，与材料同级目录；在 `topic_hub.md`「前置知识」区链接。

---

## 6. 开工前应确认

- `{source}_prereq.md` 路径；是否提供原材料摘要/目录用于做论文联动。
- 哪些前置项作者自认是"理解论文关键洞察的支点"（可由作者点名，否则由改写者按 §3 判据判定）。
- 产出粒度（默认分章/分节；预习册子通常较短，可一次成稿）。
