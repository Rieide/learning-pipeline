---
name: source-to-textbook
description: 编辑层「主教材这一路 · v1 生成」：把 source_selection.md 收敛的「确定的、范围可控的资料清单」（收料）组织成一本结构化主教材 v1——它既是比分散原始文档更好读的【主要阅读材料】，也是后续 QA 融合的【底本】；再交通用渲染层 tool_prompts/to_tex.md 渲染为可编译 .tex。与「预习路」prereq_to_textbook.md、「成稿/融合路」note_to_textbook.md 并列三编辑层、共用同一渲染层。本文件只管"把收料组织成主教材内容"；LaTeX 样式/编译/通用教科书味标准见 to_tex.md。可选步骤：源密集/分散、值得 consolidate 才生成 v1；可直读的参考文档可跳过、直接对收料 QA（走 QA 优先路）。
---

# 收料 → 主教材 v1：主教材生成层（阅读底本）

> **定位**：这是**编辑层**里「主教材这一路」的 **v1 生成**规范。三条编辑层并列、共用渲染层 `tool_prompts/to_tex.md`：
> - 「预习路」`prereq_to_textbook.md`：差集 → 预习教材；
> - **「主教材生成路」本文件**：收料 → 结构化主教材 **v1**（阅读底本）；
> - 「成稿/融合路」`note_to_textbook.md`：QA → 融合进 v1 → **v2**（个性化）。
> **用法：把本文件 + `tool_prompts/to_tex.md` 一起作为上下文。** 本文件管「收料怎么组织成主教材、覆盖到什么程度、按什么边界」，`to_tex.md` 管「怎么写得有教科书味 + 排版/绘图/编译」。
> **在流程中的位置**（见 `README.md`）：上游 `source_selection.md` 产出 `{topic}_sources.md`（收料）→ **本步（收料 → 主教材 v1）** → 下游 `qa_note.md`（读 v1 边读边 QA，落 `{topic}_qa/`）→ … → `note_to_textbook.md`（把 QA 融合回 v1 → v2）。

---

## 0. 目标（一句话）

把 `source_selection.md` 选定且裁剪过的**收料**，组织成一本**结构化主教材 v1**：
它要同时胜任两个角色——① **主要阅读材料**（比分散在多处的原始文档更好读、把多源 consolidate 成一条连贯主线）；
② QA 融合的**底本**（后续 `note_to_textbook.md` 把你的 QA 与思考嵌回它，原地长成 v2）。

**两个不可动摇的落点：**
- **grounded 在收料、服务 objective**：内容忠实于选定收料、覆盖其主干以达成 topic 的 objective；不外扩成领域百科、不复述收料之外的东西。
- **这是 v1，不是终稿**：个性化（贴合作者盲点）由后续 QA 融合（v2）完成。本步**不臆测作者盲点、不替他发散**——只把收料"讲清楚、讲连贯"。

---

## 1. 输入

- `{topic}_sources.md`（`source_selection.md` 的产出：选定且裁剪过的收料清单 + objective 拆解）。
- 各选定源的实际内容（按裁剪范围读：指定的节/页/函数/文档页）——**主教材内容必须 grounded 在这些真实源**，不靠记忆杜撰。
- 背景文件（`$PERSONAL_BACKGROUND`）：差集已剔除 `S_have`，本步沿用，不重复讲作者已会的。

---

## 2. 何时生成 v1（可选步骤，按源类型决定）

本步**可选**，与「预习路」一样按 ROI 取舍：

| 源的性质 | 是否生成 v1 主教材 |
|---|---|
| **密集/难读**（论文、复杂算法源码） | **生成**：结构化读本远好读过裸论文，ROI 高 |
| **分散多处但单份可读**（手册 + wiki + 文档，需 consolidate） | **生成**：把多源拢成一条连贯主线，有 consolidation 价值 |
| **单一、可直读的参考文档**（一份 API 手册） | **可跳过**：直接对收料 QA（QA 优先路），让 `note_to_textbook` 从 QA 建教材 |

> 跳过本步时，流程退化为「QA 优先」：`qa_note` 直接对收料跑 → `qa_to_archive` → `note_to_textbook` 从零成稿（见 `note_to_textbook.md` 的模式 A）。

---

## 3. 两个正交控制轴（与预习路同精神，对象换成主内容）

### 轴 1 · 学习范围（守住）
内容边界 = 收料覆盖 objective 所需的主干。不外扩成第二本百科、不引入收料之外的新内容；多源之间做 consolidate（去重叠、连主线），而非平铺堆叠。

### 轴 2 · 讲解深度（按 objective 重要性分流）
| 内容性质 | 讲解深度 | 形式 |
|---|---|---|
| **objective 支点**（理解/动手该 topic 的关键） | 逻辑链完整展开：动机→机制→怎么用，并给可操作细节 | 正文主线 + 图 + 表 + `keybox` |
| **次要/边角**（知道即可） | 点到为止 | 简短说明 / `notebox` |

**grounded 要求**：命令/公式/接口/报告格式等**可核验的细节**，一律以**真实源**为准（标清来自哪份收料），不臆造。

---

## 4. 组织

- **先整体后细节、问题驱动**：每章先给"这块解决什么、在 topic 全局的位置"，再展开（遵循 `to_tex.md §6` 教科书味标准）。
- **consolidate 多源**：把分散在不同源里的同一主题内容并到一条主线，而不是按"源"分章。
- **为 QA 融合留位**：v1 是底本，行文清楚、模块边界分明，便于后续把 QA（疑问/卡点/纠错）就近嵌进对应小节长成 v2。
- **明确标注 v1**：在读前说明里写清"这是 v1 / 主要阅读材料 / 待 QA 融合为 v2"，并交代来源（哪些收料）。

---

## 5. 渲染 / 编译 / 样式 → 见渲染层

全部遵循 `tool_prompts/to_tex.md`，本文件不重复排版规范：
- 通用「教科书味」写作标准见其 §6；预导言/TikZ/画图避坑见其 §1–§3；编译自检见其 §5（XeLaTeX 连两遍 + 查 `.log` + 确认 PDF）。
- 产物命名：主教材 `{topic}_Textbook.tex`（**v1 与 v2 是同一文件的演进，不另起名**）；其随文 QA 落 `{topic}_qa/`。在 `topic_hub.md`「主材料组 / 最终产物」区链接。

---

## 6. 开工前应确认

- `{topic}_sources.md` 路径；各选定源能否取到实际内容（用于 grounded 取材）。
- 是否确实需要 v1（按 §2 源类型判据；可直读单源可跳过）。
- 哪些是 objective 支点（作者可点名，否则按 §3 判据判定）。
- 产出粒度（默认分章、逐章编译；主教材通常多章，按章推进便于校验）。
