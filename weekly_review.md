---
name: weekly-review
description: 时间驱动的周复盘回填入口。把"本周/本里程碑的工作叙事"（做了什么/卡在哪/学到什么/产出结论）同步进两个背景文档——详细流水账写 WORK_LOG.md，真正内化的能力提炼进 $PERSONAL_BACKGROUND 的知识资产台账并滚动刷新能力现状表。与 background_update.md 互补：那是主题驱动（一个深读主题收尾，输入是教科书/复习卡/目标问题），本入口是时间驱动（按周/里程碑，输入是原始工作叙事）。台账/能力表/变更记录的写法与硬边界一律复用 background_update.md，本文件只补"周复盘特有"的部分。
---

# 周复盘回填（时间驱动的背景同步入口）

> **定位**：想把"这一段时间（一周 / 一个里程碑）干了什么、学到什么"同步进背景文档时的入口。与 `background_update.md` 互补，**不重复**它：
> - `background_update.md`：**主题驱动**，输入是一个深读主题的产物（教科书/复习卡/目标问题），主题收尾时跑。
> - 本 prompt：**时间驱动**，输入是一段时间的原始工作叙事，按周/里程碑跑。
> 两者共用同一套"写回背景"的格式与纪律——**台账 / 能力现状表 / 变更记录的规则一律以 `background_update.md` §3/§4/§5 为准**，这里只补周复盘语境特有的几点。

---

## 0. 输入与产出

- **输入**：你口述/粘贴的本周工作叙事——做了什么、卡在哪、请教到什么、产出与结论。可零散、可流水。
- **产出（两个文档，按变动频率分流写）**：
  - **`WORK_LOG.md`**（动态日志，与 `$PERSONAL_BACKGROUND` 同目录）：详细周进度时间线条目，保留过程、卡点、结论。
  - **`$PERSONAL_BACKGROUND`**（稳定档案）：仅"真正内化为能力"的部分 → 知识资产台账 + 能力现状表 + 变更记录（格式见 `background_update.md`）。

---

## 1. 步骤

### 步骤 A — 写 `WORK_LOG.md`（详细流水账）
1. 在「进度时间线（按周）」顶部新增一条 `### 本周（YYYY-MM-DD ~ MM-DD）：<一句话主题>`。
2. 按天或按主题组织过程：**做了什么 → 卡点 → 怎么突破 → 结论**。保留"卡在哪、怎么解决"——这是复盘价值，别抹掉。
3. **排障级细节**（具体命令、坐标、通道数、报错栈…）压成一句并指向对应操作手册，不在此展开（见 `WORK_LOG.md` 顶部体例）。
4. 同步更新「学习/补强计划」里相关条目的进度与下一步。

### 步骤 B — 提炼能力写回 `$PERSONAL_BACKGROUND`
> 格式、颗粒度、"仍存疑"负向知识、硬边界，**完全照 `background_update.md` §2/§3/§4/§5 执行**。周复盘语境下额外强调三点：
1. **做差分**：只记"相对本周开始前**新增/纠正**"的能力；本周只是"做了"却没内化成可迁移能力的，留在 `WORK_LOG.md`，不进台账。
2. **区分"做过"与"会了"**：完成一个 case ≠ 掌握一类方法。拿不准就记进"仍存疑/未深入"，宁可下次差集多补，也别拔高。
3. **滚动刷新能力现状表**：把本轮台账新增汇总进对应行（**台账为准**），不动「入职基线」锚点。

---

## 2. 硬边界（周复盘特有；其余同 `background_update.md` §5）
- ❌ **不把工作叙事整段塞进台账**：台账只要"可被差集相减的能力 + 存疑清单"，过程留在 `WORK_LOG.md`。
- ❌ **不臆测掌握程度**：凡你没明确说"已掌握"的，按"仍存疑"处理，或**直接问作者**，不要替他拔高。
- ❌ **不让进度污染稳定档案**：周进度/TODO 只进 `WORK_LOG.md`，稳定事实（身份/出身/入职基线）不动；能力表与台账冲突以台账为准。

---

## 3. 入口 prompt（粘贴即用）

```
读 $LEARNING_PIPELINE/weekly_review.md，按它做「周复盘回填」。

【本周复盘】
<粘贴：本周每天做了什么、卡在哪、学到/请教到什么、产出与结论>

要求：
1) WORK_LOG.md：新增本周时间线条目（按天/主题）；排障级细节压成一句并指向对应手册，不展开。
2) $PERSONAL_BACKGROUND：把"真正内化为能力"的部分提炼进「知识资产台账」（可被差集相减的细颗粒 + 显式记"仍存疑/未深入"），据此滚动刷新「能力现状表」、在「变更记录」加一行。
纪律：① 稳定事实（身份/出身/入职基线）不动；② 进度/TODO 只进 WORK_LOG，不写回稳定档案；
③ 能力表与台账冲突以台账为准；④ 拿不准我是否真掌握、或某结论是否准确，先问我，别替我臆测掌握程度。
```

---

## 4. 开工前应确认
- 本周时间范围（YYYY-MM-DD ~ MM-DD）。
- 叙事里哪些是"已掌握"、哪些只是"做过 / 仍存疑"——拿不准逐条问。
- 是否有结论需核实（如问题定位结论、某交付是否完成）。
