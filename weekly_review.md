---
name: weekly-review
stage: 时间驱动
when: 按周/里程碑，把一段时间的工作叙事沉淀
reads: 工作叙事
writes: WORK_LOG.md + s_have(have-add)
description: 时间驱动的周复盘回填入口。把"本周/本里程碑的工作叙事"（做了什么/卡在哪/学到什么/产出结论）分流写回：详细流水账写 WORK_LOG.md，真正内化的能力用 background.py have-add 幂等写进 s_have 表（按领域 key）。与 background_update.md 互补：那是主题驱动（一个深读主题收尾，输入是教科书/复习卡/目标问题），本入口是时间驱动（按周/里程碑，输入是原始工作叙事）。两入口共写同一 s_have 表，幂等 upsert 天然防重复计入（#6）。台账写法/字段/硬边界一律复用 background_update.md + background_db.md，本文件只补"周复盘特有"的部分。
---

# 周复盘回填（时间驱动的背景同步入口）

> **定位**：想把"这一段时间（一周 / 一个里程碑）干了什么、学到什么"同步进背景文档时的入口。与 `background_update.md` 互补，**不重复**它：
> - `background_update.md`：**主题驱动**，输入是一个深读主题的产物（教科书/复习卡/目标问题），主题收尾时跑。
> - 本 prompt：**时间驱动**，输入是一段时间的原始工作叙事，按周/里程碑跑。
> 两者共用同一套"写回 `s_have` 表"的机制与纪律——**字段/枚举/CLI 见 `background_db.md`，语义规则以 `background_update.md` §2/§3/§5 为准**，这里只补周复盘语境特有的几点。**两入口共写同一表、`have-add` 幂等，不会双计**（#6）。

---

## 0. 输入与产出

- **输入**：你口述/粘贴的本周工作叙事——做了什么、卡在哪、请教到什么、产出与结论。可零散、可流水。
- **产出（按性质分流写，见 `background_db.md §1`）**：
  - **`WORK_LOG.md`**（动态日志）：详细周进度时间线条目，保留过程、卡点、结论。
  - **`s_have` 表**（`background.db`）：仅"真正内化为能力"的部分 → `have-add` 幂等写入（按领域 key、标 `mastered/shaky`、`--source weekly`）。能力现状表/变更记录都是脚本生成/自动留痕，无需手写。

---

## 1. 步骤

### 步骤 A — 写 `WORK_LOG.md`（详细流水账）
1. 在「进度时间线（按周）」顶部新增一条 `### 本周（YYYY-MM-DD ~ MM-DD）：<一句话主题>`。
2. 按天或按主题组织过程：**做了什么 → 卡点 → 怎么突破 → 结论**。保留"卡在哪、怎么解决"——这是复盘价值，别抹掉。
3. **排障级细节**（具体命令、坐标、通道数、报错栈…）压成一句并指向对应操作手册，不在此展开（见 `WORK_LOG.md` 顶部体例）。
4. 同步更新「学习/补强计划」里相关条目的进度与下一步。

### 步骤 B — 提炼能力 `have-add` 进 `s_have` 表
> 颗粒度、"仍存疑"负向知识、字段与硬边界，**照 `background_update.md` §2/§3/§5 + `background_db.md` 执行**（统一 `--source weekly`）。周复盘语境额外强调三点：
1. **做差分**：只记"相对本周开始前**新增/纠正**"的能力；本周只是"做了"却没内化成可迁移能力的，留在 `WORK_LOG.md`，不进 `s_have`。
2. **区分"做过"与"会了"**：完成一个 case ≠ 掌握一类方法。拿不准就 `--status shaky`，宁可下次差集多补，也别拔高。
3. **不必管能力现状表**：它是 `have-table` 生成的视图，`have-add` 后重渲染即可；不动 `baseline.yaml` 的入职基线锚点。

---

## 2. 硬边界（周复盘特有；其余同 `background_update.md` §5）
- ❌ **不把工作叙事整段塞进 DB**：`s_have` 只要"可被差集相减的能力 + 存疑项"，过程留在 `WORK_LOG.md`。
- ❌ **不臆测掌握程度**：凡你没明确说"已掌握"的，`--status shaky` 或**直接问作者**，不要替他拔高。
- ❌ **不让进度污染 DB**：周进度/TODO 只进 `WORK_LOG.md`；基线锚点（身份/出身/入职基线）在 `baseline.yaml`、本步不动。

---

## 3. 入口 prompt（粘贴即用）

```
读 $LEARNING_PIPELINE/weekly_review.md，按它做「周复盘回填」（字段/CLI 见 background_db.md）。

【本周复盘】
<粘贴：本周每天做了什么、卡在哪、学到/请教到什么、产出与结论>

要求：
1) WORK_LOG.md：新增本周时间线条目（按天/主题）；排障级细节压成一句并指向对应手册，不展开。
2) s_have 表：把"真正内化为能力"的部分用 background.py have-add 幂等写入（按 --domain 归位、可相减的细颗粒、--status mastered|shaky、--source weekly、能挂 topic 就 --topic）；完后 have-table 重渲染能力现状表。
纪律：① 基线（身份/出身/入职基线）在 baseline.yaml、不动；② 进度/TODO 只进 WORK_LOG，不写进 DB；
③ 能力现状表是视图、不手维护；④ 拿不准我是否真掌握、或某结论是否准确，先问我，别替我臆测（拿不准用 shaky）。
```

---

## 4. 开工前应确认
- 本周时间范围（YYYY-MM-DD ~ MM-DD）。
- 叙事里哪些是"已掌握"、哪些只是"做过 / 仍存疑"——拿不准逐条问。
- 是否有结论需核实（如问题定位结论、某交付是否完成）。
