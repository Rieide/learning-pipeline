---
name: to-review-cards
description: 把已沉淀的学习材料（{source}_qa/ 里单文件 QA 的问答、教科书 .tex 里的 warnbox/keybox、阅读目标问题）转成「间隔复习卡」的规范。产出 Markdown 表格形式的问/答卡，可在 obsidian 里盖住答案列自测。目的：给以编码为主的流水线补上「主动提取 + 间隔重复」回路，对抗"读着顺=已掌握"的流畅性幻觉。复用已有材料，零额外采集成本。
---

# 学习材料 → 间隔复习卡（Markdown 表格）

> **定位**：单主题深读流程的**收尾回路**，在 `note_to_textbook.md` 产出教科书之后（或并行）。
> **认知依据**：再读/再整理只增强"再认流畅度"，会制造**掌握的幻觉**；真正促成长期保持与迁移的是**主动提取（retrieval practice）+ 间隔重复（spacing）**。本步把流水线从"单向编码产出"补成"学习闭环"。
> **零成本来源**：复习卡的原料已经存在——`{source}_qa/` 里每个 QA 文件的 Q/A、教科书里的 `warnbox`（误区）/`keybox`（关键概念）、`prereq_and_objectives.md` 的阅读目标问题，本就是现成的提取项。

---

## 0. 目标（一句话）

把已有学习材料里**值得长期记住的东西**，提炼成一组**能盖住答案自测的问/答卡**，按"误区/关键概念/为什么/对照"组织，供间隔复习。

---

## 1. 输入（按优先级取料）

1. `{source}_qa/`（单文件 QA） —— 作者真实问过的问题，天然是好提取项（可读各 QA 的 `summary`/正文）。
2. 教科书 `.tex` 的 `warnbox`/`tbox`（误区→澄清）、`keybox`（关键概念）—— 直接转卡。
3. `prereq_and_objectives.md` 的阅读目标问题 —— 每个目标问题至少出一张卡，检验是否真达成。

> **按组适用（通用）**：若该主题分了主组 / 预习组，本步对每组分别取料、各出一份卡（如 `{topic}_review` / `{topic}_预习_review`），便于复习时区分"基础底子"与"topic 专属"。卡的「来源」列本就记 QA id / 教科书出处，天然标明属哪组。无分组时就出一份，逻辑不变——本步只认被指向的取料范围，与"组"无关。

---

## 2. 出卡原则（不许出"再认题"）

- **考提取，不考再认**：问题要逼出"在脑子里重建答案"，而不是"看一眼就眼熟"。
- **优先级**：误区/反直觉 > 设计动机（为什么这么做）> 关键机制/公式 > 名词定义。**少出纯名词背诵卡。**
- **一卡一点**：每张卡只问一个可独立回忆的点；大问题拆成几张。
- **答案自包含**：答案列要能独立看懂，不依赖"上一张卡"。
- **对照入卡**：与已有知识的横向对照（如 SIMPL vs MTR/VectorNet 在某点的异同+为什么），是高价值卡，单独出。
- **可回溯**：每张卡标来源（哪个 QA 文件 id / 教科书哪节），便于复习时回查。

---

## 3. 产出格式（Markdown 表格，盖列自测）

文件名 `{topic}_review.md`，与材料同级目录。结构：

```markdown
# {topic} 复习卡
> 用法：盖住「答」列，先自己回忆，再揭开核对。难卡标记后下一轮优先。

## 一、常见误区（warnbox 来源 + 纠错痕迹）
| # | 问 | 答 | 来源 |
|---|---|---|---|
| 1 | … | … | QA_0017 |

## 二、为什么 / 设计动机
| # | 问 | 答 | 来源 |
|---|---|---|---|

## 三、关键机制 / 公式
| # | 问 | 答 | 来源 |
|---|---|---|---|

## 四、横向对照（与已有知识）
| # | 问 | 答 | 来源 |
|---|---|---|---|
```

> 分节顺序即复习优先级：先攻误区与动机，再机制与对照。
> 若某点适合填空，写成"问"里挖空、"答"给完整句即可，仍用同一张表，不引入外部工具。

---

## 4. 间隔复习的轻量约定（可选，不强制工具）

- 在 obsidian 枢纽 `review` 区记三次复习时点（如 +1d / +3d / +7d 当日勾选），靠 frontmatter/手记即可，不必上 Anki。
- 复习时把"想不起来/答错"的卡 `#` 记下，下一轮提到表格最前面。

---

## 5. 硬边界

- ❌ 不外扩：只就已有材料里出现过的内容出卡，不引入新知识。
- ❌ 不把整段讲解原样塞进答案列——答案要是"被提取出的要点"，不是再读一遍。
- ✅ 出卡可以重新措辞问题（和"原文归档"那步的铁律不同：复习卡的目的就是变换形式促提取），但答案不得歪曲原材料结论。

---

## 6. 开工前应确认

- 取料范围（哪个 `{source}_qa/` / 哪份教科书）。
- 是否要为每个阅读目标问题强制各出一张卡。
